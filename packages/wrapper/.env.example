# Optional — WalletConnect Cloud project id (free: https://cloud.walletconnect.com).
# MetaMask / injected wallets work without it. The registry address is hardcoded (Sepolia).
NEXT_PUBLIC_WALLETCONNECT_ID=

# Optional — override the default Sepolia RPC.
NEXT_PUBLIC_SEPOLIA_RPC=
