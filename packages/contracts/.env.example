# Wallet that deploys + demos (Sepolia). Needs Sepolia ETH.
PRIVATE_KEY=

# Any Sepolia RPC. Default public node works for light use.
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Etherscan API key — only needed for `npx hardhat verify`.
ETHERSCAN_API_KEY=
